# Core Django dependencies
Django==5.1.7
djangorestframework==3.15.2
django-cors-headers==4.7.0
channels==4.2.0  # Keep if using Django Channels for WebSockets
sqlparse==0.5.3

# Database dependencies
pymongo==4.11.2  # Keep if using MongoDB
djongo==1.2.31  # Only needed if using MongoDB with Django

# Environment & config
python-dotenv==1.0.1

# HTTP & Requests
requests==2.32.3
httpx==0.28.1
urllib3==2.3.0
idna==3.10
charset-normalizer==3.4.1
certifi==2025.1.31

# Security
cryptography==44.0.2
pyOpenSSL==25.0.0

# WebSocket & Async Support (needed for Django Channels)
attrs==25.3.0
Twisted==24.11.0
autobahn==24.4.2
txaio==23.1.1
Automat==24.8.1
service-identity==24.2.0

# Template Engine
MarkupSafe==3.0.2
Jinja2==3.1.6

# Data Validation
pydantic==2.10.6
pydantic_core==2.27.2

# OpenAI & Whisper Dependencies
openai==1.66.3
openai-whisper==20240930
tiktoken==0.9.0

# Audio Processing (for Whisper)
pydub==0.25.1
soundfile==0.13.1

# Miscellaneous
regex==2024.11.6
tqdm==4.67.1

# Optional Dependencies (remove if not needed)
ffmpeg-python==0.2.0  # Needed for audio processing (if using Whisper)
networkx==3.4.2  # Graph processing (if needed)
numpy==2.1.3  # Needed for data science or Whisper
sympy==1.13.1  # Needed for symbolic mathematics